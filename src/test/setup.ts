// Test setup file
// Note: @testing-library/jest-dom requires jsdom environment
// For node environment (utility tests), we skip this import
// For component tests that need jsdom, uncomment the line below
// import '@testing-library/jest-dom'
